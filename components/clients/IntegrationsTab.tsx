'use client';

export default function IntegrationsTab({ clientId }: { clientId: string }) {
  return (
    <div>
      <h2 className="font-semibold text-gray-900 dark:text-white mb-4">Integrations</h2>
      <p className="text-gray-500">Integrations tab content coming soon</p>
    </div>
  );
}
