'use client';

export default function TeamTab({ clientId }: { clientId: string }) {
  return (
    <div>
      <h2 className="font-semibold text-gray-900 dark:text-white mb-4">Team</h2>
      <p className="text-gray-500">Team tab content coming soon</p>
    </div>
  );
}
